import * as repository from './dashboard.repository';

export const resumenAdmin = repository.resumenAdmin;
export const resumenProfesor = repository.resumenProfesor;
export const resumenAlumno = repository.resumenAlumno;
