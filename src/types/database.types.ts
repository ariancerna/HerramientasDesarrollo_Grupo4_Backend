/**
 * TODO (equipo BD): reemplazar este archivo por el que genera Supabase:
 *   npx supabase gen types typescript --project-id <PROJECT_ID> --schema public > src/types/database.types.ts
 * Mientras tanto, dejamos "any" para no bloquear el arranque del proyecto.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
